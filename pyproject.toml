[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "my_project"
version = "0.1.0"
description = "A Python project using Pillow"
dependencies = [
    "pillow==9.0.1",
    "pandas",
    "matplotlib",
    "textblob"
]
